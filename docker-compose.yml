#services:
#  db:
#    image: postgres:18-alpine
#    container_name: inventory-db
#    environment:
#      POSTGRES_DB: ${DB_NAME:-inventory}
#      POSTGRES_USER: ${DB_USER:-postgres}
#      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
#    ports:
#      - "${DB_PUBLISHED_PORT:-55432}:5432"
#    volumes:
#      - dbdata:/var/lib/postgresql
#    healthcheck:
#      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-inventory}"]
#      interval: 5s
#      timeout: 3s
#      retries: 12
#      start_period: 10s
#
#  backend:
#    build:
#      context: .
#      dockerfile: Dockerfile
#    image: inventory-backend:db
#    container_name: inventory-backend
#    depends_on:
#      db:
#        condition: service_healthy
#    environment:
#      DB_HOST: db
#      DB_PORT: 5432
#      DB_NAME: ${DB_NAME:-inventory}
#      DB_USER: ${DB_USER:-postgres}
#      DB_PASSWORD: ${DB_PASSWORD:-postgres}
#    ports:
#      - "8080:8080"
#    restart: on-failure
#
#volumes:
#  # Mounted at /var/lib/postgresql rather than at .../data: the postgres 18 image keeps its cluster
#  # in a version-named subdirectory and refuses to start against the older layout. Named dbdata so
#  # it does not collide with a pgdata volume left over from that layout.
#  dbdata: